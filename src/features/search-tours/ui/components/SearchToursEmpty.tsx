import styles from "./SearchToursEmpty.module.scss";

const SearchToursEmpty = () => {
  return <div className={styles.empty}>За вашим запитом турів не знайдено</div>;
};

export default SearchToursEmpty;
